# 改善方針

## リポジトリ概要

`Utayomi` は、短歌の一覧を指定したLLMに入力し、評（コメント）や要約、選評を生成するシステムです。

主要スクリプト:
- `pipeline.py`: 全評モード・歌会モード・連作モードの処理を担うCLI
- `selection.py`: 選評モードのCLI

主要モジュール:
- `lib/arrange_prompt.py`、`lib/model_input.py`、`lib/tanka_critic.py`、`lib/submodules/*`: 各モデルに応じたプロンプト生成・入出力処理
- 設定ファイル: `model_conf.yaml`（全評／歌会モード）、`model_selection_conf.yaml`（選評モード）
- 入力: CSV（短歌一覧）
- 出力: CSV（評結果）、Markdown/PDF（歌会モード、選評モード）

## 入出力フロー

1. CLI引数で入力CSV・出力ディレクトリ・設定識別子・モードを指定
2. 設定ファイルからモデルごとのパラメータを読み込み
3. `output_preprocess` で入力CSVを前処理し（一時ファイル対応）、Pandas DataFrameで読み込み
4. 各行の短歌データに対して `tanka_critic` 等を呼び出し、プロンプト生成 → モデル呼び出し → 出力取得
5. 一時CSVに追記し、最後にCSVをマージ
6. モードに応じてMarkdown/PDFを生成

## 課題・問題点

- **コードのモジュール化・再利用**  
  - `pipeline.py` と `selection.py` で類似処理が散見される  
  - `lib/submodules/` 以下のプロンプト・ツール関係の分散と結合度の高さ
- **設定ファイルの整合性**  
  - YAMLのキー命名規則や大文字小文字、スネークケースの揺れ  
  - 全評用／選評用で設定フォーマットが若干異なる
- **プロンプト生成部の肥大化・重複**  
  - テンプレートがソース直書きで可読性・保守性が低い  
  - 未定義変数や重複コードの混在（`arrange_prompt.py`、`prompt_generate.py`、`tanka_prompt.py`）
- **エラーハンドリング・ログ**  
  - `exit()` と `print()` による制御、例外処理とログ出力が散乱  
  - `subprocess.run` での外部コマンド呼び出しを直接埋め込み
- **パフォーマンス・並列化**  
  - 一件ずつのAPIコール／逐次書き込みで高速化の余地大  
  - バッチ処理や非同期処理が未導入
- **依存関係・環境構築**  
  - `install.md` と `requirements.txt` のズレ  
  - Dockerfile・CI設定が無く再現性が低い
- **テスト・CI・品質管理**  
  - ユニットテストやLint設定が未整備  
  - 型ヒント・静的解析が未導入

## 改善方針

1. **リファクタリングとモジュール化**  
   - CLI処理の共通化（引数パース→設定読み込み→前処理→ループ→後処理）  
   - ビジネスロジックを責務別に分割し、再利用性を向上
2. **設定ファイルの統一・バリデーション**  
   - Pydantic や YAMLスキーマで設定フォーマットを定義し、キー命名規則を統一  
   - 全評用／選評用を可能な限り同一形式に統一
3. **プロンプトテンプレートの管理改善**  
   - Jinja2 等のテンプレートエンジン導入による外部ファイル化  
   - ソース直書きを避け、テンプレートを集中管理
4. **ロギングとエラーハンドリング**  
   - 標準 `logging` モジュールでログレベル制御  
   - 例外捕捉・詳細ログ出力とリトライ制御の実装
5. **パフォーマンス最適化**  
   - Hugging Face のバッチ生成や GPU メモリ管理の整理  
   - 並列／非同期処理導入によるAPIコール高速化
6. **開発環境・再現性の向上**  
   - Dockerfile / docker-compose で環境を定義  
   - GitHub Actions などで CI（Lint, 型検査, テスト, ビルド）を構築
7. **テスト・品質管理**  
   - pytest によるユニットテスト・統合テストの整備  
   - 型ヒント(PEP484)、mypy導入  
   - pre-commit フック(black, flake8, isort)の導入
8. **ドキュメント整備**  
   - README/INSTALL/CONTRIBUTING をコード変更に合わせて更新  
   - Sphinx や MkDocs でAPIリファレンス自動生成
9. **モデルキャッシュ管理・配信**  
   - Hugging Faceキャッシュ、GGUFモデルの自動ダウンロードとCI検証  
   - モデルアセットをGit LFSや別ストレージで管理

## 今後のタスク

- [x] CLI共通モジュール抽出
- [ ] 設定スキーマ定義と Pydantic 導入
- [ ] プロンプト管理基盤の構築
- [ ] ロギング・例外処理整備
- [ ] パフォーマンステスト＆並列化対応
- [ ] Docker 環境整備
- [ ] CI パイプライン構築
- [ ] テスト自動化＆型チェック
- [ ] ドキュメント自動生成